# Layers of Youth Digital Safety — Image Generation Prompt

Please generate a new image.

## Critical Rule

**The only text permitted in this image is the short section header printed at the top of each panel, listed explicitly below. Nothing else.** No body copy, no bullet points, no captions, no numbers, no arrows, no callout lines, and no overall poster title. All of the explanatory content is added afterwards as an interactive HTML overlay from a separate data file, and any extra text baked into the image will collide with it.

---

## Image Specifications

- **Format**: PNG
- **Filename to save as**: `youth-safety-layers.png`
- **Dimensions**: 1000 × 800 px (landscape)
- **Background**: clean flat white (#FFFFFF)
- **Style**: flat vector layered-stack poster, clean shield and lock pictograms, muted textbook palette
- **Audience**: a textbook for coding club leaders, mentors, and older students

---

## What to Draw

A stacked-band poster showing five layers of youth digital safety, drawn as five full-width horizontal bands from the bottom of the frame to the top. The bottom band is the foundation and the top band is the daily habit, so the stack reads like a wall built from the ground up. The five printed band titles, and the ONLY text in the image, are, from BOTTOM to TOP: "Device Security Policy", "Password Management for Kids", "Parental Control Software", "Content Filtering Software", "Screen Time Management".

### 1. Device Security Policy

**Position**: The bottom band, roughly 80% to 96% down the image.

**Visual**: Solid blue title block at the left end. To its right: a document icon with a small padlock on it, and a laptop icon with a lock badge.

**Panel accent colour**: `#4A90D9` -- tint this panel's background, header bar, or icons with it

### 2. Password Management for Kids

**Position**: The second band from the bottom, roughly 62% to 78% down the image.

**Visual**: Solid green title block at the left end. To its right: a key icon, and a row of four large dots representing a masked passphrase (dots only, no letters).

**Panel accent colour**: `#50C878` -- tint this panel's background, header bar, or icons with it

### 3. Parental Control Software

**Position**: The middle band, roughly 44% to 60% down the image.

**Visual**: Solid amber title block at the left end. To its right: an app-grid icon with one tile crossed out, an hourglass, and a small bar-chart icon.

**Panel accent colour**: `#F5A623` -- tint this panel's background, header bar, or icons with it

### 4. Content Filtering Software

**Position**: The fourth band from the bottom, roughly 26% to 42% down the image.

**Visual**: Solid rose-red title block at the left end. To its right: a funnel or sieve icon with shapes passing through and one blocked, plus a small router/wifi icon.

**Panel accent colour**: `#E85D75` -- tint this panel's background, header bar, or icons with it

### 5. Screen Time Management

**Position**: The top band, roughly 8% to 24% down the image.

**Visual**: Solid violet title block at the left end. To its right: a simple alternating timeline of two shapes -- a screen icon then a hands/tools icon then a screen icon -- suggesting alternation.

**Panel accent colour**: `#9B6BC7` -- tint this panel's background, header bar, or icons with it

---

## Layout Notes

Draw the 5 bands as full-width horizontal strips stacked with a small uniform gap of plain background between each pair. Every band must span the SAME left and right edges (about 3% to 97% of the image width) and have the SAME height, so the overlay's rectangular hover zones line up with them exactly. Each band carries its printed title in bold dark text at its LEFT end, on a solid block of that band's accent colour, with simple flat icons in the remaining space to the right. No other text anywhere. Because the stack is a foundation metaphor, draw the bottom band slightly thicker and visually heaviest, with the bands becoming lighter in visual weight toward the top.

**Reminder, because image models routinely ignore it:** **The only text permitted in this image is the short section header printed at the top of each panel, listed explicitly below. Nothing else.** No body copy, no bullet points, no captions, no numbers, no arrows, no callout lines, and no overall poster title. All of the explanatory content is added afterwards as an interactive HTML overlay from a separate data file, and any extra text baked into the image will collide with it.
