# Three Curriculum Tracks — Image Generation Prompt

Please generate a new image.

## Critical Rule

**The only text permitted in this image is the short section header printed at the top of each panel, listed explicitly below. Nothing else.** No body copy, no bullet points, no captions, no numbers, no arrows, no callout lines, and no overall poster title. All of the explanatory content is added afterwards as an interactive HTML overlay from a separate data file, and any extra text baked into the image will collide with it.

---

## Image Specifications

- **Format**: PNG
- **Filename to save as**: `three-curriculum-tracks.png`
- **Dimensions**: 1200 × 800 px (landscape)
- **Background**: clean flat white (#FFFFFF)
- **Style**: flat vector comparison poster, clean geometric icons, muted textbook palette, generous white space
- **Audience**: a textbook for coding club leaders, mentors, and older students

---

## What to Draw

A three-column comparison poster about coding club curriculum levels. 

The left "Beginner" column has white text on a green header.

The center "Intermediate" column has white text on a blue header.

The right column has white text on a black background.


Each column is a tall rounded panel with a colored header bar carrying its printed title, and icons and images below the title. The three printed headers, and the ONLY text in the image, are: "Beginner Track", "Intermediate Track", "Advanced Track".  The remaining text will be drawn on an overlaying layer for user event mouse and hover region monitoring.

### 1. Beginner Track

**Position**: The left column, roughly 2% to 31% of the image width.

**Visual**: Header bar tinted green. Below it: a keyboard icon, two interlocking puzzle-piece code blocks, and a small cat-like sprite shape that imply a block-programming environment.

**Panel accent color**: green -- tint this header bar, or icons with it

### 2. Intermediate Track

**Position**: The centre column, roughly 35% to 65% of the image width.

**Visual**: Header bar blue. Below it: a puzzle-piece block on the left with an arrow pointing right to a plain text-line icon, plus a small turtle shape trailing a simple spiral path.

**Panel accent color**: blue -- tint this panel's background, header bar, or icons with it.

### 3. Advanced Track

**Position**: The right column, roughly 68% to 98% of the image width.

**Visual**: Header bar black. Below it: several small colorful code-block shapes assembling into one larger rounded shape, plus two figures side by side at one screen suggesting pair programming on a complex task.  They are wearing colorful clothes.

**Panel accent color**: black -- tint this panel's header bar with it

---

## Layout Notes

Divide the content area into 3 equal-width vertical columns with a thin pale divider line or a clear gutter of background between each pair. Every column must have the SAME width, the SAME top edge, and the SAME bottom edge, so the overlay's rectangular hover zones line up with them exactly. Each column has its printed header in bold dark text at the very top of the column, on a tinted header bar in that column's accent color. Below the header, fill the column with simple flat ICONS and abstract shapes only -- no words, no bullet points, no sentences. Leave a clear margin of at least 4% of the image width on the left and right edges and 8% of the height at the top and bottom.

**Reminder, because image models routinely ignore it:** **The only text permitted in this image is the short section header printed at the top of each panel, listed explicitly below. Nothing else.** No body copy, no bullet points, no captions, no numbers, no arrows, no callout lines, and no overall poster title. All of the explanatory content is added afterwards as an interactive HTML overlay from a separate data file, and any extra text baked into the image will collide with it.
