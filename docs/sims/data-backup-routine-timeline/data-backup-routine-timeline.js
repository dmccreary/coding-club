// Data Backup Routine Timeline
// CANVAS_HEIGHT: 600
//
// Generated by scripts/build-timeline-sim.py from
// scripts/sim-timelines/data-backup-routine-timeline.json -- edit the data file, not this.

const CLASS_COLORS = {
    "hub": [
        "#F5A623",
        "#B87B12",
        "#000000"
    ],
    "node": [
        "#4A90D9",
        "#2E6BA8",
        "#FFFFFF"
    ],
    "resolve": [
        "#3E9E6E",
        "#256E4A",
        "#FFFFFF"
    ],
    "danger": [
        "#D64545",
        "#8F2B2B",
        "#FFFFFF"
    ],
    "wait": [
        "#8E7CC3",
        "#5B4B8A",
        "#FFFFFF"
    ],
    "remediate": [
        "#E8833A",
        "#A85718",
        "#FFFFFF"
    ],
    "neutral": [
        "#5B7186",
        "#33475B",
        "#FFFFFF"
    ]
};

const itemData = [
    {
        "id": "w1",
        "at": 1,
        "cls": "hub",
        "label": "Confirm sync is on",
        "hint": "Week 1 — about 10 minutes",
        "detail": [
            [
                "What it involves",
                "Open the tracking spreadsheet and the consent-form folder, and confirm each one is actually syncing to cloud backup. Not \"should be\" — look at the sync status."
            ],
            [
                "How long",
                "About ten minutes, once per semester."
            ],
            [
                "Why week 1",
                "Everything after this assumes backups are running. If this step is skipped, the next fifteen weeks of automatic backup are backing up nothing."
            ]
        ]
    },
    {
        "id": "w4",
        "at": 4,
        "cls": "node",
        "label": "Backup running",
        "hint": "Week 4 — no action needed",
        "detail": [
            [
                "What it involves",
                "Nothing. The backup runs on its own."
            ],
            [
                "Why it is on the timeline at all",
                "Because an automatic backup you never look at feels identical to one that stopped in week 2. Marking the quiet weeks is what makes the noisy ones meaningful."
            ]
        ]
    },
    {
        "id": "w8",
        "at": 8,
        "cls": "hub",
        "label": "Mid-semester restore test",
        "hint": "Week 8 — about 20 minutes",
        "detail": [
            [
                "What it involves",
                "Restore one real file from the backup and open it. Not check that the backup exists — restore it, and read it."
            ],
            [
                "How long",
                "About twenty minutes."
            ],
            [
                "Why this is the important one",
                "A backup that cannot be restored is not a backup, and it fails silently. Corrupted archives, expired permissions, and a sync that quietly stopped all look exactly like a working backup until the day you need one."
            ],
            [
                "The rule",
                "A backup you have never restored from is a hypothesis, not a backup."
            ]
        ]
    },
    {
        "id": "w12",
        "at": 12,
        "cls": "node",
        "label": "Backup running",
        "hint": "Week 12 — no action needed",
        "detail": [
            [
                "What it involves",
                "Nothing again. Four more quiet weeks."
            ],
            [
                "Worth noticing",
                "Of sixteen weeks, only two need a person. The cadence is cheap precisely because it is mostly automatic."
            ]
        ]
    },
    {
        "id": "w16",
        "at": 16,
        "cls": "hub",
        "label": "Restore test + archive",
        "hint": "Week 16 — about 45 minutes",
        "detail": [
            [
                "What it involves",
                "Verify the backups again the same way as week 8, then archive the semester's records according to the retention schedule."
            ],
            [
                "How long",
                "About forty-five minutes, most of it the archiving rather than the test."
            ],
            [
                "Why the two are joined",
                "The end of a semester is the moment the retention clock starts on a lot of records. Doing the archive at the same time as the check means the deletion dates get set while somebody is already looking at the data."
            ]
        ]
    }
];
const TICKS = {
    "1": "Week 1",
    "4": "Week 4",
    "8": "Week 8",
    "12": "Week 12",
    "16": "Week 16"
};
const MARKER = null;
const WINDOW_MIN = -2;
const WINDOW_MAX = 19;
const TIMELINE_HEIGHT = 220;

let timeline, items;

// vis-timeline works in dates, and none of these timelines is about dates: one
// counts years since founding, one counts weeks of a semester, one counts down
// to an event. Ordinal positions are mapped onto a fixed epoch and the axis is
// relabelled from TICKS, so the reader never sees a calendar.
const EPOCH = Date.UTC(2000, 0, 1);
const UNIT = 24 * 60 * 60 * 1000;          // one ordinal step = one day

function toDate(n) { return new Date(EPOCH + n * UNIT); }
// vis-timeline hands the axis formatter a Moment and the move handler a Date.
// valueOf() is the one accessor both have.
function toOrdinal(d) { return Math.round((d.valueOf() - EPOCH) / UNIT); }

function styleFor(cls) {
    const c = CLASS_COLORS[cls] || CLASS_COLORS.neutral;
    return 'background-color:' + c[0] + '; border-color:' + c[1] +
           '; color:' + c[2] + ';';
}

function buildItems() {
    const out = itemData.map(it => {
        const o = {
            id: it.id,
            content: it.label,
            start: toDate(it.at),
            type: it.end !== undefined ? 'range' : 'box',
            style: styleFor(it.cls),
            title: it.hint || it.label,
            editable: false
        };
        // Only set `end` when there really is one. vis-timeline's DataSet
        // type-converts every key that is present, so an explicit
        // `end: undefined` makes it call getTime() on undefined and the
        // whole timeline fails to draw.
        if (it.end !== undefined) o.end = toDate(it.end);
        return o;
    });
    if (MARKER) {
        out.push({
            id: '__marker',
            content: MARKER.label,
            start: toDate(MARKER.at),
            type: 'point',
            className: 'marker',
            // The only draggable thing on the timeline: the reader's own
            // bookmark. Everything else is fixed so a stray drag cannot
            // rearrange the content.
            editable: { updateTime: true, updateGroup: false, remove: false }
        });
    }
    return out;
}

// The window is fixed -- no panning, no zooming -- so position maps to pixels
// linearly across the centre panel, and a tick can be placed exactly on the
// gridline it names.
function layoutTicks() {
    const host = document.getElementById('axis-ticks');
    const panel = document.querySelector('#timeline .vis-panel.vis-center');
    if (!host || !panel) return;
    const hr = host.getBoundingClientRect();
    const pr = panel.getBoundingClientRect();
    const span = WINDOW_MAX - WINDOW_MIN;
    host.innerHTML = '';
    Object.keys(TICKS).forEach(function (k) {
        const frac = (Number(k) - WINDOW_MIN) / span;
        const el = document.createElement('span');
        el.textContent = TICKS[k];
        el.style.left = ((pr.left - hr.left) + frac * pr.width) + 'px';
        host.appendChild(el);
    });
}

function initializeTimeline() {
    items = new vis.DataSet(buildItems());

    const options = {
        height: TIMELINE_HEIGHT + 'px',
        min: toDate(WINDOW_MIN),
        max: toDate(WINDOW_MAX),
        start: toDate(WINDOW_MIN),
        end: toDate(WINDOW_MAX),
        // Locked window: there is nothing outside the range, so panning or
        // zooming out of it only loses the reader.
        zoomable: false,
        moveable: false,
        selectable: true,
        stack: true,
        align: 'center',
        margin: { item: 12, axis: 18 },
        orientation: { axis: 'bottom', item: 'top' },
        // One tick per ordinal position. Left to itself vis picks a round
        // step -- 4 days across a 19-day window -- and any label whose
        // position is not a multiple of that step silently never renders.
        // Ticking every position and returning '' for the unlabelled ones
        // puts the TICKS map in charge of what the reader sees.
        timeAxis: { scale: 'day', step: 1 },
        editable: { updateTime: true, add: false, remove: false },
        onMove: function (item, callback) {
            // Snap the bookmark to whole positions, and never let it leave the
            // range.
            let n = Math.round(toOrdinal(item.start));
            n = Math.max(Math.ceil(WINDOW_MIN), Math.min(Math.floor(WINDOW_MAX), n));
            item.start = toDate(n);
            callback(item);
        },
        format: {
            minorLabels: function () { return ''; },
            majorLabels: function () { return ''; }
        }
    };

    const container = document.getElementById('timeline');
    timeline = new vis.Timeline(container, items, options);
    timeline.on('select', onSelect);
    timeline.on('changed', layoutTicks);
    window.addEventListener('resize', layoutTicks);
    layoutTicks();
}

function onSelect(props) {
    const id = props.items && props.items[0];
    if (!id || id === '__marker') { clearInfo(); return; }
    const it = itemData.find(d => d.id === id);
    if (!it) return;
    const body = (it.detail || [])
        .map(pair => '<p><strong>' + pair[0] + ':</strong> ' + pair[1] + '</p>')
        .join('');
    document.getElementById('info-content').innerHTML =
        '<div class="info-label" style="color:' +
        (CLASS_COLORS[it.cls] || CLASS_COLORS.neutral)[1] + '">' +
        it.label.replace(/<[^>]*>/g, ' ') + '</div>' + body;
}

function clearInfo() {
    document.getElementById('info-content').innerHTML =
        '<p class="info-placeholder">Click any checkpoint for what it involves and roughly how long it takes. The two amber ones are the only dates anyone has to put in a calendar.</p>';
}

function reset() {
    timeline.destroy();
    initializeTimeline();
    clearInfo();
}

document.addEventListener('DOMContentLoaded', function () {
    initializeTimeline();
    document.getElementById('reset-btn').addEventListener('click', reset);
});
