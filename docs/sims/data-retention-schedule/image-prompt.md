# Data Retention Schedule by Type — Image Generation Prompt

Please generate a new image.

## Critical Rule

**The only text permitted in this image is the short section header printed at the top of each panel, listed explicitly below. Nothing else.** No body copy, no bullet points, no captions, no numbers, no arrows, no callout lines, and no overall poster title. All of the explanatory content is added afterwards as an interactive HTML overlay from a separate data file, and any extra text baked into the image will collide with it.

---

## Image Specifications

- **Format**: PNG
- **Filename to save as**: `data-retention-schedule.png`
- **Dimensions**: 1000 × 700 px (landscape)
- **Background**: clean flat white (#FFFFFF)
- **Style**: flat vector schedule poster, clean document and clock pictograms, muted textbook palette
- **Audience**: a textbook for coding club leaders, mentors, and older students

---

## What to Draw

A stacked-band poster showing how long four kinds of club data are kept, drawn as four full-width horizontal bands. The bands are ordered by retention length: the BOTTOM band is kept longest and the TOP band is kept in anonymised form only. The four printed band titles, and the ONLY text in the image, are, from BOTTOM to TOP: "Consent Forms", "Progress Records", "Contact Information", "Survey Responses".

### 1. Consent Forms

**Position**: The bottom band, roughly 76% to 96% down the image.

**Visual**: Solid blue title block at the left end. To its right: a document icon with a signature squiggle on it, linked by a short chain-link symbol to a small folder icon.

**Panel accent colour**: `#4A90D9` -- tint this panel's background, header bar, or icons with it

### 2. Progress Records

**Position**: The second band from the bottom, roughly 52% to 74% down.

**Visual**: Solid green title block at the left end. To its right: a badge rosette icon, a small grid/checklist icon, and an hourglass. No numbers on any of them.

**Panel accent colour**: `#50C878` -- tint this panel's background, header bar, or icons with it

### 3. Contact Information

**Position**: The third band from the bottom, roughly 28% to 50% down.

**Visual**: Solid amber title block at the left end. To its right: an envelope icon and a simple person silhouette, with a small trash-bin icon at the right end of the band.

**Panel accent colour**: `#F5A623` -- tint this panel's background, header bar, or icons with it

### 4. Survey Responses

**Position**: The top band, roughly 4% to 26% down.

**Visual**: Solid violet title block at the left end. To its right: several small speech-bubble icons merging into one bar-chart icon, and a person silhouette with a line through it to suggest anonymisation.

**Panel accent colour**: `#9B6BC7` -- tint this panel's background, header bar, or icons with it

---

## Layout Notes

Draw the 4 bands as full-width horizontal strips stacked with a small uniform gap of plain background between each pair. Every band must span the SAME left and right edges (about 3% to 97% of the image width), so the overlay's hover zones line up exactly. No text other than the printed titles listed above. Each band carries its printed title in bold dark text at its LEFT end on a solid block of that band's accent colour, with simple flat pictograms to the right. Do NOT draw any durations, dates, month counts, or numbers anywhere -- the retention periods are supplied by the overlay.

**Reminder, because image models routinely ignore it:** **The only text permitted in this image is the short section header printed at the top of each panel, listed explicitly below. Nothing else.** No body copy, no bullet points, no captions, no numbers, no arrows, no callout lines, and no overall poster title. All of the explanatory content is added afterwards as an interactive HTML overlay from a separate data file, and any extra text baked into the image will collide with it.
