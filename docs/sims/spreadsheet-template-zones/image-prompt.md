# Spreadsheet Template Zones — Image Generation Prompt

Please generate a new image.

## Critical Rule

**The only text permitted in this image is the short section header printed at the top of each panel, listed explicitly below. Nothing else.** No body copy, no bullet points, no captions, no numbers, no arrows, no callout lines, and no overall poster title. All of the explanatory content is added afterwards as an interactive HTML overlay from a separate data file, and any extra text baked into the image will collide with it.

---

## Image Specifications

- **Format**: PNG
- **Filename to save as**: `spreadsheet-template-zones.png`
- **Dimensions**: 1000 × 500 px (landscape)
- **Background**: clean flat white (#FFFFFF)
- **Style**: flat vector mock-spreadsheet illustration, clean grid lines, muted textbook palette
- **Audience**: a textbook for coding club leaders, mentors, and older students

---

## What to Draw

A mock tracking spreadsheet drawn as a clean grid, filling the frame. It has one header row across the top and about five data rows beneath it. The columns are grouped into three visually distinct blocks separated by a slightly heavier vertical divider line: a NARROW left block of two columns, a WIDE centre block of about six columns, and a NARROW right block of one column. Each block's header cells are tinted in that block's accent colour. The ONLY text in the image is the short header words in the header row: in the left block "Name" and "Joined"; in the centre block six short badge names "First Program", "Loops", "Variables", "Functions", "Sensors", "Debugging"; in the right block "Notes".

### 1. Identity columns

**Position**: The narrow left block of two columns, roughly 3% to 30% of the image width.

**Visual**: Two columns with blue-tinted header cells reading "Name" and "Joined", with short neutral grey placeholder bars in the cells beneath.

**Panel accent colour**: `#4A90D9` -- tint this panel's background, header bar, or icons with it

### 2. Badge columns

**Position**: The wide centre block of about six columns, roughly 32% to 75% of the image width.

**Visual**: Six narrow columns with green-tinted header cells carrying the six short badge names, with small date-like dashes in some cells and blanks in others, creating a visible staircase of completion from left to right.

**Panel accent colour**: `#50C878` -- tint this panel's background, header bar, or icons with it

### 3. Notes column

**Position**: The narrow right block of one wide column, roughly 77% to 97% of the image width.

**Visual**: A single wide column with an amber-tinted header cell reading "Notes", its cells containing longer neutral grey placeholder bars suggesting free text.

**Panel accent colour**: `#F5A623` -- tint this panel's background, header bar, or icons with it

---

## Layout Notes

Draw the three column blocks so their left and right boundaries are unambiguous, with a heavier divider between blocks than between the columns inside a block, so the overlay's rectangular hover zones line up with the block boundaries exactly. The left block should span roughly 3% to 30% of the image width, the centre block roughly 32% to 75%, and the right block roughly 77% to 97%. Fill the data cells with neutral grey placeholder bars and small tick or short date-like dashes -- do NOT write real names, real dates, or any readable content in the data cells. Only the header row carries readable words.

**Reminder, because image models routinely ignore it:** **The only text permitted in this image is the short section header printed at the top of each panel, listed explicitly below. Nothing else.** No body copy, no bullet points, no captions, no numbers, no arrows, no callout lines, and no overall poster title. All of the explanatory content is added afterwards as an interactive HTML overlay from a separate data file, and any extra text baked into the image will collide with it.
