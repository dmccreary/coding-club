# Showcase Wall vs Peer Recognition Activity — Image Generation Prompt

Please generate a new image.

## Critical Rule

**The only text permitted in this image is the short section header printed at the top of each panel, listed explicitly below. Nothing else.** No body copy, no bullet points, no captions, no numbers, no arrows, no callout lines, and no overall poster title. All of the explanatory content is added afterwards as an interactive HTML overlay from a separate data file, and any extra text baked into the image will collide with it.

---

## Image Specifications

- **Format**: PNG
- **Filename to save as**: `showcase-wall-vs-peer-recognition.png`
- **Dimensions**: 1000 × 800 px (landscape)
- **Background**: clean flat white (#FFFFFF)
- **Style**: flat vector comparison poster, warm friendly icons, muted textbook palette
- **Audience**: a textbook for coding club leaders, mentors, and older students

---

## What to Draw

A two-column comparison poster about two ways of recognising student work. Each column is a tall rounded panel with a coloured header bar carrying its printed title, and a clean flat illustration below. The two printed headers, and the ONLY text in the image, are: "Student Showcase Wall" (left) and "Peer Recognition Activity" (right).

### 1. Student Showcase Wall

**Position**: The left column, roughly 4% to 47% of the image width.

**Visual**: Header bar tinted blue. Below it: a cork bulletin board on a wall with six pinned cards showing small abstract project thumbnails and blank caption bars beneath each (no readable words), with two adult visitor silhouettes looking at it from one side.

**Panel accent colour**: `#4A90D9` -- tint this panel's background, header bar, or icons with it

### 2. Peer Recognition Activity

**Position**: The right column, roughly 53% to 96% of the image width.

**Visual**: Header bar tinted green. Below it: five student figures seated in a circle facing inward, with small speech bubbles and a couple of small hand-passed cards between them, and a small clock icon showing a short duration.

**Panel accent colour**: `#2ECC71` -- tint this panel's background, header bar, or icons with it

---

## Layout Notes

Divide the content area into 2 equal-width vertical columns with a clear gutter of background between each pair. Every column must have the SAME width, top edge, and bottom edge so the overlay's hover zones line up exactly. Each column has its printed header in bold dark text at the very top, on a tinted header bar in that column's accent colour, with simple flat icons below. No other text anywhere. Leave at least 4% margin at the left and right edges and 8% at top and bottom. With only two columns each panel is wide, so draw one larger scene per column rather than a cluster of small icons.

**Reminder, because image models routinely ignore it:** **The only text permitted in this image is the short section header printed at the top of each panel, listed explicitly below. Nothing else.** No body copy, no bullet points, no captions, no numbers, no arrows, no callout lines, and no overall poster title. All of the explanatory content is added afterwards as an interactive HTML overlay from a separate data file, and any extra text baked into the image will collide with it.
